// Generated DB types placeholder.
// The data layer (packages/db/src/data/*) uses hand-written Row interfaces and
// does NOT import `Database`, so generated types are optional. To regenerate
// (requires Docker for the local generator, or `supabase login` for --linked):
//   supabase gen types typescript --linked > packages/db/src/types.ts
export type Database = Record<string, never>;
